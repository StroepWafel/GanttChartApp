{
  "name": "gantt-chart-app",
  "version": "2.2.0",
  "description": "Gantt chart web application with optional auth and read-only IoT API",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:android": "npm run setup:android && npm run build:mobile",
    "build:ios": "npm run setup:ios && node scripts/build-mobile.js --ios",
    "build:mobile": "node scripts/build-mobile.js",
    "setup:android": "node scripts/setup-android.js",
    "setup:ios": "node scripts/setup-ios.js",
    "start": "cd backend && npm start",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix mobile install",
    "update": "bash scripts/update.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.3.1",
    "sharp": "^0.34.5",
    "sharp-ico": "^0.1.5"
  }
}