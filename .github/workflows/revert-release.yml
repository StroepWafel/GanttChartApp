name: Revert Release

on:
  workflow_dispatch:
    inputs:
      revert_to_tag:
        description: 'Tag to revert to (e.g. v2.6.1 or 2.6.1). "v" prefix added if missing.'
        required: true
        type: string
      tag_to_delete:
        description: 'Tag to delete from remote (e.g. v2.6.2 or 2.6.2). "v" prefix added if missing. Leave empty to skip.'
        required: false
        type: string

jobs:
  revert:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Validate revert tag exists
        id: tag
        run: |
          RAW="${{ github.event.inputs.revert_to_tag }}"
          [[ "$RAW" == v* ]] && TAG="$RAW" || TAG="v$RAW"
          if ! git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Error: Tag $TAG does not exist"
            exit 1
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Restore package.json from tag
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          VER="${TAG#v}"
          echo "Restoring package.json files to version $VER from $TAG"
          for f in package.json backend/package.json frontend/package.json; do
            if [ -f "$f" ]; then
              git show "$TAG:$f" > "$f"
              echo "Restored $f"
            fi
          done

      - name: Commit and push revert
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json backend/package.json frontend/package.json
          git status
          if git diff --staged --quiet; then
            echo "No changes to commit - already at $TAG version"
            exit 0
          fi
          git commit -m "Revert release: restore to $TAG"
          git push origin HEAD

      - name: Delete tag and release (if specified)
        if: github.event.inputs.tag_to_delete != ''
        run: |
          RAW="${{ github.event.inputs.tag_to_delete }}"
          [[ "$RAW" == v* ]] && TAG="$RAW" || TAG="v$RAW"
          echo "Deleting tag and release: $TAG"
          gh release delete "$TAG" --yes 2>/dev/null || true
          git push origin --delete "$TAG" 2>/dev/null || true
          echo "Done. Tag and release $TAG removed."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
