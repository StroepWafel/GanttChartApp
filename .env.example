# Server
PORT=3001

# Optional auth (set AUTH_ENABLED=true to enable)
AUTH_ENABLED=false
AUTH_EMAIL=admin@example.com
AUTH_PASSWORD=changeme

# Optional API key for read-only API (leave empty for no auth)
API_KEY=

# Database path (relative to backend)
DB_PATH=./data/gantt.db
