# Server
PORT=3001
# HOST=0.0.0.0  # Listen on all interfaces (default). Use 127.0.0.1 to restrict to localhost.
# TRUST_PROXY=1  # Set when behind nginx/Cloudflare etc. (avoids rate-limit proxy errors)

# Optional auth (set AUTH_ENABLED=true to enable)
AUTH_ENABLED=false
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme
# Optional: AUTH_PASSWORD_HASH=bcrypt_hash (if you don't want plaintext in .env)
JWT_SECRET=your-secret-change-in-production

# Database path (relative to backend)
DB_PATH=./data/gantt.db

# For update check (optional): GitHub repo owner/name, e.g. StroepWafel/GanttChartApp
# GITHUB_REPO=owner/repo
# Optional: GitHub token (increases rate limit to 5,000/hr; or set in Settings → Updates)
# GITHUB_TOKEN=ghp_...

# Mobile app (optional): build Capacitor Android APK when server updates
# Set MOBILE_APP_ENABLED=true and PUBLIC_URL to enable. Admin must also enable in Settings → App.
# MOBILE_APP_ENABLED=false
# PUBLIC_URL=https://gantt.example.com
# Optional: JAVA_HOME for Gradle (Android Gradle Plugin needs Java 11+). If unset, the build script searches for JDK 17.
# Example (Windows, Android Studio): JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
# Example (Linux, snap): JAVA_HOME=/snap/android-studio/current/android-studio/jbr
# Example (Linux, apt): JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
# Optional: ANDROID_HOME for Android SDK. Default: ~/Android/Sdk (Linux/Mac) or %LOCALAPPDATA%\Android\Sdk (Windows)

